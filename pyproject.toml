[project]
name = "qprl"
version = "0.1.0"
description = "PostgreSQL plan-steering research harness"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
qprl = "qprl.cli:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
