# Development-only values. Replace these before starting the container.
QPRL_POSTGRES_PASSWORD="replace-with-a-long-random-admin-password"
QPRL_AGENT_PASSWORD="replace-with-a-long-random-agent-password"

# Optional local overrides:
QPRL_POSTGRES_PORT="55432"
